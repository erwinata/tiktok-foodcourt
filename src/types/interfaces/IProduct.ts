export interface IProduct {
  id: number;
  photo: string;
  nama: string;
  stok: number;
  harga: number;
  hargaAsli: number;
  deskripsi: string;
  kategoriId: number;
  status: string;
  // createdAt: Date;
  // updatedAt: Date;
  // isFavorite: boolean;
}
